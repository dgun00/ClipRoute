spring:
  application:
    name: "CLIPROUTE"

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # MySQL JDBC ???? ??? ??
    url: jdbc:mysql://localhost:3306/cliproute_test #${DB_URL}
    # jdbc:mysql://localhost:3306/{???????}
    username: root #${DB_USER}
    # MySQL ?? ??
    password: gksehdrjs1@ #${DB_PW}
    # MySQL ????

  jpa:
    database: mysql # ??? ?????? ?? ?? (MySQL)
    database-platform: org.hibernate.dialect.MySQLDialect # Hibernate?? ??? MySQL ??(dialect) ??
    show-sql: true # ??? SQL ??? ??? ???? ?? ??
    hibernate:
      ddl-auto: update # check [update]
    properties:
      hibernate:
        format_sql: true # ???? SQL ??? ?? ?? ???